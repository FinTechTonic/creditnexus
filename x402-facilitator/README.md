# x402 Facilitator (moved)

**This project has been moved to `demo_mcp/facilitator/`.**

Use the facilitator from the demo tree:

```bash
cd demo_mcp/facilitator
npm install
npm run build
node dist/index.js
```

See `demo_mcp/facilitator/README.md` and `dev/DEPLOY_X402_FACILITATOR.md` for setup and deployment.
